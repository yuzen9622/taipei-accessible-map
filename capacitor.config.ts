import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.accessiblemap.app',
  appName: 'accessible-map-frontend',
  webDir: 'out'
};

export default config;
